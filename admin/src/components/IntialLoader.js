import React, {Component} from "react";

class IntitialLoader extends Component {
    render () {
        return (
            <div className="initial-loader" />
        )
    }
}

export default IntitialLoader;